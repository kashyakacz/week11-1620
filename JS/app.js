// your JavaScript goes here, you can delete this comment
let todoList = { todo: "clone repo for starter code", status: "complete, started" };
let completedTodos = {};


for (let i = 0; i < todoList.length; i++) {
    if (todoList[i] === 'complete') {
        completedTodos == todoList[index]

    }
}

for (let i = 0; i< completedTodos.length; i++) {
    console.log(i)
}